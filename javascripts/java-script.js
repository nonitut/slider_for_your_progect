$(document).ready(function(){
    $('.slider').slick({
      // Настройки слайдера
      autoplay: true, // автоматическое пролистывание
      autoplaySpeed: 2000, // скорость автоматического пролистывания (в миллисекундах)
      dots: true, // показывать точки навигации
      arrows: true // показывать стрелки навигации
    });
});